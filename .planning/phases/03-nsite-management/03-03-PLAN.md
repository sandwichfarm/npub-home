---
phase: 03-nsite-management
plan: 03
type: execute
wave: 3
depends_on:
  - 03-02
files_modified:
  - src/routes/+page.svelte
autonomous: false
requirements:
  - NSITE-01
  - NSITE-02
  - NSITE-03
  - NSITE-04

must_haves:
  truths:
    - "NsiteList in +page.svelte receives isOwner, signer, pool, and writeRelays props"
    - "Owner sees edit and delete controls on nsites after login"
    - "Visitors see no edit/delete controls (isOwner=false when not logged in)"
    - "writeRelays passed to NsiteList is the same relays already computed for ThemePicker"
  artifacts:
    - path: "src/routes/+page.svelte"
      provides: "NsiteList wired with all management props"
      contains: "isOwner={isOwner()}"
  key_links:
    - from: "src/routes/+page.svelte"
      to: "src/lib/components/NsiteList.svelte"
      via: "isOwner, signer, pool, writeRelays props"
      pattern: "NsiteList.*isOwner"
---

<objective>
Wire the management props (isOwner, signer, pool, writeRelays) into the NsiteList usage in +page.svelte. This is a targeted one-file change that activates the edit/delete UI for the owner.

Purpose: NsiteList already has the implementation (Plan 02) but receives no management props yet. +page.svelte already computes writeRelays for ThemePicker — NsiteList reuses those exact values.

Output: NsiteList in +page.svelte passes all four management props; the phase is functionally complete.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-nsite-management/03-02-SUMMARY.md

@src/routes/+page.svelte
@src/lib/components/NsiteList.svelte
</context>

<interfaces>
<!-- Current NsiteList usage in +page.svelte (line 139) -->
```svelte
<NsiteList {nsites} {host} {pubkey} />
```

<!-- Target after this plan -->
```svelte
<NsiteList
  {nsites}
  {host}
  {pubkey}
  isOwner={isOwner()}
  signer={getSigner() ?? undefined}
  {pool}
  {writeRelays}
/>
```

<!-- Already imported in +page.svelte (no new imports needed) -->
<!-- isOwner, getSigner — from '$lib/auth.svelte' (line 11) -->
<!-- pool — from '$lib/nostr/store' (line 16) -->
<!-- writeRelays — $state<string[]>([]) (line 27) -->
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Wire NsiteList management props in +page.svelte</name>
  <files>src/routes/+page.svelte</files>
  <read_first>src/routes/+page.svelte</read_first>
  <action>
    Locate the single `<NsiteList {nsites} {host} {pubkey} />` usage (currently line 139). Replace it with:

    ```svelte
    <NsiteList
      {nsites}
      {host}
      {pubkey}
      isOwner={isOwner()}
      signer={getSigner() ?? undefined}
      {pool}
      {writeRelays}
    />
    ```

    No other changes to +page.svelte are needed:
    - `isOwner` and `getSigner` are already imported from `$lib/auth.svelte`
    - `pool` is already imported from `$lib/nostr/store`
    - `writeRelays` is already a `$state<string[]>([])` variable maintained by the relaysSub subscription
    - `getSigner()!` is safe in ThemePicker (inside isOwner guard); for NsiteList use `getSigner() ?? undefined` to be explicit — NsiteList.svelte guards internally before using signer

    Do NOT change any other part of +page.svelte.
  </action>
  <verify>
    <automated>npx vitest run 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep -A 6 "NsiteList" src/routes/+page.svelte` shows isOwner, signer, pool, writeRelays props
    - `npx tsc --noEmit` passes with no errors
    - Full test suite (`npx vitest run`) passes — no regressions
  </acceptance_criteria>
  <done>NsiteList in +page.svelte has all four management props; TypeScript check passes; full test suite green</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Verify Phase 3 edit and delete UI in browser</name>
  <action>Human visually verifies the complete edit and delete owner management UI in the running app.</action>
  <verify>Human confirms all verification steps below pass.</verify>
  <done>User types "approved" after confirming edit, delete, and visitor-view behavior are correct.</done>
  <what-built>
    Complete Phase 3: NsiteEntry extended with sourceEvent, NsiteList with edit/delete UI, +page.svelte wired. Owner can now edit nsite name/description and request NIP-09 deletion from the live page.
  </what-built>
  <how-to-verify>
    1. Start the dev server: `npm run dev` — open the page in browser on a local nsite domain or the dev hostname
    2. Log in as owner (NIP-07 extension) — owner badge appears
    3. Verify edit icons (✏) and delete icons (🗑) appear next to each nsite entry
    4. Click ✏ on an nsite — verify inline form opens with current name and description pre-filled
    5. Change the name, click Save — verify form collapses (save event fires; relay echo will update the displayed name)
    6. Click 🗑 on an nsite — verify "Request deletion? This is best-effort." message appears with Delete + Cancel buttons
    7. Click Cancel — verify row returns to normal view
    8. Log out — verify no edit/delete icons visible on nsites
    9. Visitor view (logged out) — verify the nsite list looks identical to pre-Phase-3 (cards with arrow, no icons)
  </how-to-verify>
  <resume-signal>Type "approved" if the edit/delete UI works as described, or describe any issues for follow-up</resume-signal>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` — no TypeScript errors
2. `npx vitest run` — full suite passes
3. `grep "isOwner" src/routes/+page.svelte` — shows NsiteList prop wiring
4. Manual visual check per checkpoint above
</verification>

<success_criteria>
- NsiteList in +page.svelte receives isOwner, signer, pool, writeRelays
- TypeScript compiles cleanly
- Full test suite passes
- Owner sees edit/delete controls after login
- Visitors see clean read-only nsite list
- Phase 3 success criteria from ROADMAP.md all met:
  1. Owner can edit nsite name (kind 35128/15128 republish via EventFactory.modify)
  2. Owner can edit nsite description (same flow)
  3. Owner can request deletion (kind 5 via setDeleteEvents)
  4. Deletion UI labels action "Request deletion" with best-effort explanation
</success_criteria>

<output>
After completion, create `.planning/phases/03-nsite-management/03-03-SUMMARY.md`
</output>
